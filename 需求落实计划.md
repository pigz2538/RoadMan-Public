# RoadMan 用户需求落实计划

本文件把最近反馈拆成可验收的实现项；每项完成后都要有自动化测试、可观测事件和一次真实运行验证。代码实现以 Agent 的语义判断为主，规则只负责安全边界、数据校验和无模型时的保守降级。

## 0. 需求理解与日期安全边界

- [x] Requirement Agent 负责从中文、英文和混合表达中提取出发地、目的地、体验目标、交通方式、人数、偏好和特殊活动；“到九宫山看流星雨”拆为目的地“九宫山”与活动“流星雨”。
- [x] 结构化日期兜底覆盖具体日期、今天/明天/后天/前天、周末、周一至周五、next Sunday、this weekend 等相对表达，并保证返回日期不早于出发日期；模型不可用时只补日历结构，不猜景点和人数。
- [x] “情侣/夫妻”等人数由模型语义判断；没有证据时显示待确认，不把运行时默认 1 人冒充用户输入。
- [x] 需求预检先完成逐题确认，再创建 Trip；过去日期、倒置日期、跨海方式、不可执行时间窗和缺失地点必须可解释地回问。

## 1. 行程读取与规划状态

- [x] 历史行程直接读取已保存快照；完成/失败行程不重播 SSE 动画，进行中的任务只从当前游标接入。
- [x] 规划页刷新时保持 canonical Trip 与活动队列一致，不因轮询重复追加或整页重绘。
- [x] 规划进度拆为可观察的检索、天气、候选复核、日程复核、校验、渲染、持久化子步骤；候选复核有心跳、超时降级和单调递增进度，不允许卡在 87%。

## 2. 真实目的地研究与 FlyAI

- [x] 新增目的地研究 Agent：使用公开网页检索和 FlyAI `keyword-search`/`ai-search`，收集必去景点、代表性美食、公开来源和图片/详情链接。
- [x] POI Agent 结合高德、OpenTripMap/OpenStreetMap、FlyAI 和目的地研究结果做去重、中文化、知名度排序；低质量候选只作为备选。
- [x] 目的地研究返回的代表性景点不再被酒店距离分数压过：对研究 Agent 标记的名胜保留优先级，必要时按来源名称回查高德坐标，并按剩余天数分配到每日景点槽位；本地接驳选点同样优先覆盖城市级名胜，未把城市景点名单硬编码进程序。
- [x] 目的地名胜覆盖流程输出片区、建议停留时长和适合时段；按片区/坐标聚类安排每日 2–4 个景点，首日、返程日同样生成可执行接驳，并在校验结果中记录已覆盖/未覆盖名胜及天数不足原因。
- [x] FlyAI 结构化景点、餐饮、酒店调用保留独立事件、调用审计、缓存和降级原因；记录 API/CLI 是否真的执行。

## 3. 景点适配复核

- [x] 每个候选结合日期、天气、气温、风雨、海拔/地形、开放信息、交通方式、同行人数和偏好交给 suitability Agent。
- [x] Agent 无法返回时只做保守降级，不把某个月份硬编码成全局禁用表；每个候选仍需逐项说明适配理由。

## 4. 日程、酒店与餐饮

- [x] 同城市/同片区/短距离连续住宿优先复用同一家合格酒店；过滤青旅、青年旅舍、旅舍、hostel 等不符合舒适偏好的住宿。
- [x] 早餐、午餐、晚餐都要有合理时间窗；优先高评分/有来源的餐厅，跨餐次和跨天轮换，候选不足才重复并注明。
- [x] 景点数量、停留时长和自由时间按当天交通与营业时间动态安排；每日复核上午、下午、晚间，避免抵达景区后整段空白或全天重复同一景点。
- [x] 驾车长途按续航、休息、充电/加油和餐饮服务分段；步行/骑行显示总爬升与可行性，公共交通无驾车路线时可作为接驳方式。

## 5. 前端体验与导出

- [x] 新建/重新规划展示平滑的逐项生成动画；历史行程直接进入详情，不等待进度条。
- [x] 规划未完成时隐藏导出；完成后统一 HTML 模板生成 HTML/PDF/PPTX/长图，包含真实路线图、地图背景、不同 POI 图片、详情链接和来源。
- [x] 右侧 Agent 展示真实调用进度和可修改上下文；支持选中阶段/活动后语义增加、删除、替换和重新规划。

## 6. 回归与交付

- [x] 补充日期/相对日期/中英文、情侣人数、闭环路线、历史读取、FlyAI、酒店复用、餐厅轮换、目的地研究和导出测试。
- [x] `pytest`、前端构建、Docker 健康检查和真实 FlyAI/Ollama 冒烟通过后，更新 `project.md`；待提交后推送 `main`。

## 7. 本轮交付验收顺序

1. 先运行日期/抽取、目的地研究、旅游调度、图与 API 测试，确认错误信息和进度事件可复现。
2. 再执行前端构建和 Docker backend/worker 重建，检查 `/api/v1/skills/health`、`/metrics` 与 SSE。
3. 在容器中实际执行一次 FlyAI `keyword-search`；`ai-search` 即使超时也必须留下 `SkillCall` 审计和可解释降级，不能静默跳过。
4. 检查历史行程不会重播旧动画，重新规划会重新订阅实时事件；完成前导出接口继续返回 409。
5. 所有验收通过后只提交本次需求相关文件，推送到 `main`，并在 `project.md` 记录模型、环境变量、验证命令和已知降级。
