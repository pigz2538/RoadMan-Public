# 数据来源与合规说明

> 对应参赛材料「4. 数据来源与合规说明」。本文件说明 RoadMan 处理的**数据类型、数据来源、授权状态、脱敏方式、隐私保护、行业风险提示**，以及**不替代专业决策的边界**。RoadMan 当前是本地受控的参赛 Demo，非公网多用户服务；相关边界在文末「当前边界与后续升级」中如实声明。

---

## 1. 数据类型

| 类型 | 内容 | 处理方式 |
| --- | --- | --- |
| 用户输入 | 出行需求（自然语言文本）、附件（图片/PDF/DOCX/Markdown/XLSX）、编辑指令、地图选点 | 用于需求提取、语义编辑与上下文，保存于 Trip / 版本 / 消息状态 |
| 车辆参数 | 车型目录身份信息（品牌/车系/车型）、续航与电量、座位数 | 用户确认后保存；仅用于规划上下文，不采集车牌 |
| 对话与编辑状态 | 澄清问答、语义编辑消息、补丁预览/确认/回滚 | 保存于 `Trip`、`PlanPatch`、`TripVersion`，供恢复与回滚 |
| 外部调用结果 | 地理编码 / POI / 路线 / 天气 / 班次 / 酒店 / 餐厅 / 景点 / 来源链接 / 风险 | 归一化为领域对象，附带 `SourceRecord`（来源、URL、抓取时间、状态） |
| 附件上传 | 用户提供的文档与图片 | 存 `UPLOAD_DIR`，数据库仅保存安全文件名与元数据；扩展名/MIME 白名单校验 |

**默认不采集**：身份证、车牌、支付信息、精确家庭住址、持续位置轨迹。提示词与附件中的此类信息不会作为强制字段。

---

## 2. 数据来源与授权状态

| 数据/能力 | 来源 | 用途 | 授权状态与注意 |
| --- | --- | --- | --- |
| 高德 WebService | [https://lbs.amap.com](https://lbs.amap.com/) | 地理编码、真实驾车/步行/骑行/公交路线、路况、POI | 需 `AMAP_WEBSERVICE_KEY`，遵循高德开放平台开发者条款、配额、地图展示与缓存规则 |
| 高德 JSAPI（浏览器地图） | 高德地图 JSAPI v2 | 地图渲染、选点、路线展示 | 需 `VITE_AMAP_JSAPI_KEY` + `VITE_AMAP_SECURITY_JS_CODE`，按平台配置域名白名单；无 key 时降级 `MockRouteMap` |
| Open-Meteo | [https://open-meteo.com](https://open-meteo.com/en/docs) | 行程日期天气、降水概率、海拔参考 | 默认无 key；**非商业免费**，商业用途需申请 `apikey`（`customer-` 前缀）；数据源自多国气象机构，需按来源与许可要求引用 |
| OpenTripMap | [https://opentripmap.io](https://opentripmap.io/) | 国际与开放景点候选 | 需 `OPENTRIPMAP_API_KEY`；数据基于 OpenStreetMap（ODbL）与 Wikidata/Wikipedia，**需保留来源与署名**，遵守服务条款 |
| FlyAI / 飞猪 CLI | 飞猪开放能力（`@fly-ai/flyai-cli@1.0.16`） | 列车/航班/轮渡班次、酒店、餐饮、POI 与语义搜索 | 需 `FLYAI_API_KEY`；属商业旅行信息服务，调用范围、展示、缓存与转授权需按其商业条款；无 key 或缺 CLI 时返回 `SKILL_NOT_CONFIGURED`，有结果/无结果均如实返回 |
| 车型目录服务 | [https://tool.bitefu.net/car](https://tool.bitefu.net/car/) | 品牌/车系/车型目录身份信息 | 公开接口、无需 key；只导入目录身份，续航/能耗由用户确认或按上下文估算 |
| DeepSeek 官方 Chat Completions | [https://api-docs.deepseek.com](https://api-docs.deepseek.com/api/create-chat-completion/) | 需求理解、目的地研究、候选策展/排序/适配、语义编辑、附件理解 | 需 `DEEPSEEK_API_KEY`；使用 OpenAI 兼容协议。模型私有思维链经 `reasoning_content` 返回，**RoadMan 不保存、不写入日志/SSE/导出**；实测/日志保留遵循其数据处理条款 |
| 网页研究（公开网页） | 知乎、搜狐、163、本地宝、Klook 等（目的地研究智能体检索） | 景点/美食/片区/特殊活动候选与摘要 | 只保留来源链接与公开摘要作为候选证据；**无明确可再利用授权的图片不打包进公开导出** |
| 浏览器定位 | 用户浏览器主动授权 | 首页当地天气 | 仅在浏览器内本地使用；用户拒绝时回退默认位置，**不长期保存精确 GPS** |

**来源记录与降级**：任何外部数据都通过 `SourceRecord` 保留来源、抓取时间与状态；估算值显式标记（如 `ENERGY_STOP_ESTIMATED`、`CHARGING_POWER_ESTIMATED`、`WEATHER_DATA_DEGRADED`），不把估算当作实时事实。缺失的班次/开放/票务/预约字段保持 `unknown` / `unavailable`，不生成虚构编号、价格或开放状态。

> 关于 `SourceRecord.license`：当前仅 Open-Meteo 来源填写了许可证字段（项目记为 `CC BY 4.0`）。提交前应建立 provider 配置表，为每个来源补授权/条款版本、获取时间、缓存期限与可导出性；对第三方再分发与署名要求逐项确认。

---

## 3. 脱敏方式

- **凭据**：API Key 只通过未跟踪的 `.env` 或系统环境变量注入，不进 git、SSE、日志、导出或前端运行时日志。`deploy/scripts/sync-local-secrets.ps1` 从本地 `Skills/*/apikey.txt` 同步到被忽略的 `.env`，输出时省略值。
- **日志与审计**：调用审计只保存 adapter/provider、成功状态、耗时、缓存、错误码与来源摘要，**不含 key、Cookie、请求头或请求体敏感字段**。
- **模型输出**：模型私有思维链（`reasoning_content`）不落库、不入日志；SSE 只推送可展示的阶段进度与结构化决策，不暴露内部 prompt 或模型原始输出。
- **附件**：数据库仅保存安全文件名与元数据，上传内容在 `UPLOAD_DIR`，文件名经白名单校验。
- **演示证据**：截图、视频与运行证据提交前需脱敏，隐藏密钥、个人路径与精确定位。

---

## 4. 隐私保护

- **当前形态**：本地受控的参赛 Demo，默认单用户，**没有账户体系、跨行程位置记忆或长期用户画像**。
- **附件保留**：默认保留 30 天，由 `FILE_RETENTION_DAYS` 调整；定时任务只删除 `UPLOAD_DIR` 安全边界内的文件。
- **删除与级联**：用户删除一条行程时，后端在同一数据操作中清理行程文档、消息/规划状态、保存版本、关联附件元数据、后台任务与本次行程的 Skill 调用记录，并删除安全目录内的附件实体文件——为不可恢复的硬删除，前端执行前二次确认。
- **系统健康审计**：未关联具体行程的健康审计不随单条行程删除。
- **本地边界**：本地开发默认 SQLite（`DATABASE_URL=sqlite+aiosqlite:///./roadman.db`）；生产/Compose 使用 PostgreSQL。两种方式下上传与凭据均不进入仓库。

---

## 5. 行业风险提示

路线、天气、开放时间、票务、班次、路况与价格可能变化。RoadMan 会记录来源、时间与降级状态，但导出结果**不能替代**景区、交通运营方、道路管理部门或车辆厂商的实时信息。出发前请再次核对：

- 预约与实名/分时入园规则、门票与票价区间；
- 封路、**恶劣天气**（系统以 `HEAVY_PRECIPITATION`、`EXTREME_TEMPERATURE`、`HIGH_WIND`、`LOW_VISIBILITY` 等风险码标记）；
- 补能可用性（以 `ENERGY_STOP_SCHEDULED`、`ENERGY_STOP_UNAVAILABLE`、`CHARGING_POWER_ESTIMATED` 标记，估算补能点为**待确认点**，不冒充营业中的充电站/加油站）；
- 交通班次与跨城时刻；
- 连续驾驶休息与夜间驾驶（以 `REST_STOP_SCHEDULED`、`CROSS_DAY_ARRIVAL_REST`、`NIGHT_DRIVING` 标记，长途按每日上限拆分并预留过夜）。

**不把估算冒充事实**：真实道路 geometry 不可得时不把直线当导航路线；估算补能点、模拟续航样本与示例底图均明确标注，不冒充实时真实数据。

---

## 6. 不替代专业决策的边界

RoadMan 是**出发前与行程修改阶段的规划辅助系统**，明确以下边界：

- **不接入 CAN 总线、车机控制、自动驾驶或驾驶辅助执行接口**，不控制方向、制动、动力、充电或车门。
- **不做自动驾驶决策**，不替代实时导航、道路与气象主管部门公告、交通与景区运营方信息、车辆仪表及驾驶员判断。
- 规划结果与导出文件**不得作为**自动驾驶指令、应急处置结论或车辆性能承诺。
- 真实导航、道路公告、车辆状态、充电设施可用性与驾驶员判断始终具有最终优先级。
- 模型输出必须经过结构化 Schema、坐标/路线工具与确定性验证；**模型不能覆盖硬路线、时间、能耗、闭环与车辆安全结论**（`verify_plan` 阻断项优先于模型建议，可修复问题进入最多 4 轮自动修复，仍违反硬约束才请求用户调整）。

---

## 7. 当前边界与后续升级

| 范围 | 当前（本地/受控 Demo） | 升级为公网多用户前必须补齐 |
| --- | --- | --- |
| 账号与鉴权 | 无账户体系，单用户受控 | 登录、资源级鉴权、租户隔离、更严格限流 |
| 数据主体权利 | 行程级联硬删除 | 隐私告知、数据导出、授权撤回、数据主体请求流程 |
| 位置/订单/车辆数据 | 默认不采集真实订单、位置轨迹或车辆数据 | 完整隐私告知、访问控制、保留期限与删除机制 |
| 服务承诺 | 无 SLA | 熔断、告警、容量压测、灾备 |
| 第三方授权 | 逐项披露来源与调用 | 补第三方授权/条款版本、缓存/转授权与导出再使用边界 |

> 本文件所列授权状态为撰写时依据各来源公开文档与仓库实现的判断；实际以各 provider 最新条款与 `/api/v1/skills/health` 返回状态为准。RoadMan 的本地凭据（`.env`、`Skills/*/apikey.txt`）均不与仓库同步。
