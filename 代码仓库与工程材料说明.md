# 代码仓库与工程材料说明

**🌐 语言 / Language：** [**English**](代码仓库与工程材料说明.en.md) | **中文**

> 对应参赛材料「3. 代码仓库或等价工程材料」。本文件说明 RoadMan 仓库的**运行入口、依赖关系、配置文件、示例数据、部署方式、测试方法与运行证据**，全部内容以仓库当前代码、接口、Docker 配置与评测脚本为准，不描述尚未实现的能力。系统概览见 [README.md](README.md)，架构与维护边界见 [project.md](project.md)；对外部能力所依据的官方 API 文档链接随文给出。

---

## 1. 运行入口

RoadMan 提供两个运行入口：**Docker Compose（推荐，生产风格）** 与 **本地 Conda（开发调试）**。

| 入口 | 地址 | 说明 |
| --- | --- | --- |
| Web 工作台 | `http://localhost:8080` | 统一入口，由 Nginx 托管前端并反向代理 `/api` 与 `/health` 到后端 |
| 后端接口文档（OpenAPI） | `http://localhost:8000/docs` | FastAPI 自动生成，含全部请求/响应契约 |
| 后端健康检查 | `http://127.0.0.1:8000/health` | 返回 `status`、`environment` 与已注册 Skill 名列表 |
| Skill 健康与指标 | `http://127.0.0.1:8000/api/v1/skills/health`、`/calls`、`/metrics` | 各外部能力的配置/降级状态与调用统计 |
| 统一健康检查（经 Nginx） | `http://127.0.0.1:8080/health` | 前端入口同源验证 |
| 局域网演示 | `http://<主机局域网IP>:8080` | `frontend` 以 `0.0.0.0:8080:80` 绑定所有网卡 |

> 计算与数据入口：后端 API 只在宿主机回环 `127.0.0.1:8000:8000` 暴露，供本地工具与 Playwright 使用；浏览器请求一律经 Nginx `/api/` 反向代理，避免直连后端。`proxy_buffering off` 保证 SSE（`/planning/events`）事件流不被缓冲。

---

## 2. 依赖说明

### 2.1 核心运行时

| 组件 | 版本 | 用途 |
| --- | --- | --- |
| Python | 3.11（`python:3.11-slim`；Conda `python=3.11`） | 后端 FastAPI 与 ARQ worker |
| Node.js | 22（`node:22-slim`；Conda 要求 `nodejs>=20`） | 前端 Vite 构建；承载 FlyAI CLI |
| PostgreSQL | 17（`postgres:17-alpine`） | 生产数据库 |
| Redis | 7.4（`redis:7.4-alpine`） | ARQ 任务队列 |
| Nginx | 1.27（`nginx:1.27-alpine`） | 前端静态托管与统一 Web 入口 |

### 2.2 Python 依赖（`backend/requirements.txt`）

- **后端核心**：FastAPI（≥0.115）、uvicorn、Pydantic 2（≥2.10）、pydantic-settings、SQLAlchemy 2.0 async、Alembic、aiosqlite（本地 SQLite）、asyncpg（生产 PostgreSQL）、redis、arq（worker）、httpx、structlog、jsonschema、langgraph（≥1.0）。
- **文档与导出**：pypdf、python-docx、openpyxl、reportlab、python-pptx、Pillow。
- **开发与测试**：pytest、pytest-asyncio。

后端镜像额外安装中文字体 `fonts-noto-cjk`、`fonts-arphic-ukai`，用于 PDF/长图等导出正确渲染中文。

### 2.3 Node 依赖

- 前端：`frontend/package.json`，`npm ci` 安装，`npm run build`（`vue-tsc -b && vite build`）生产构建。核心为 Vue 3.5、Vite 8、TypeScript 5.9、Pinia 3、Vue Router 4.5、TanStack Vue Query 5、AMap JSAPI loader、model-viewer。
- FlyAI CLI：`backend/node-requirements.txt` 固定 `@fly-ai/flyai-cli@1.0.16`。Docker 后端镜像从一个 `node:22-slim` 阶段全局安装该 CLI，并把 `node`、`flyai` 与 `@fly-ai` 模块复制进 Python 镜像，供 `flyai.*`（列车/航班/轮渡/酒店/POI/搜索）Skill 子进程调用。

### 2.4 外部能力与凭据

| 变量 | 用途 | 说明 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | 需求理解、目的地研究、语义编辑等云端智能体 | 必需；默认模型 `DEEPSEEK_MODEL=deepseek-v4-flash` |
| `DEEPSEEK_REASONING_EFFORT` | 思考深度 | 默认 `max`（DeepSeek 官方取值为 `low/high/max`） |
| `DEEPSEEK_THINKING` | 是否启用思考模式 | 默认 `true` |
| `DEEPSEEK_API_URL` | Chat Completions 地址 | 默认 `https://api.deepseek.com/chat/completions` |
| `AMAP_WEBSERVICE_KEY` | 地理编码、POI、真实驾车/步行/骑行/公交路线、路况 | 必需；缺失则上述能力降级 |
| `FLYAI_API_KEY` | 旅行搜索、住宿、餐饮补充 | 推荐 |
| `OPENTRIPMAP_API_KEY` | 国际/开放景点数据补充 | 可选 |
| `VITE_AMAP_JSAPI_KEY` / `VITE_AMAP_SECURITY_JS_CODE` | 浏览器真实地图 | 构建时注入，改动后需重建 frontend |
| `VITE_AMAP_SERVICE_HOST` | 高德 JS API 加载器同源代理主机 | 可选 |
| `ROADMAN_HTTP_PROXY` | 容器访问外网所需的宿主机代理 | Docker Desktop 用 `http://host.docker.internal:<port>` |
| `CORS_ORIGINS` | 允许的跨域 origin，逗号分隔 | 默认 `http://localhost:8080` |
| `POSTGRES_*` | Compose 数据库账号/库名 | 生产需覆盖默认密码；`POSTGRES_PASSWORD` 默认 `roadman_dev` |

> 权威键名与默认值见 [.env.example](.env.example) 与 `docker-compose.yml`。缺少非必需 Key 时对应能力自动降级（如无浏览器地图 Key 时前端退回 `MockRouteMap`），不影响主流程。`LOAD_LOCAL_SKILL_CREDENTIALS` 控制是否从本地读取 Skill 凭据文件；Compose 中设为 `false`。

---

## 3. 配置文件

| 文件 | 说明 |
| --- | --- |
| [.env.example](.env.example) | 全部环境变量模板与默认值；复制为 `.env` 后注入真实凭据 |
| [docker-compose.yml](docker-compose.yml) | 定义 `postgres` / `redis` / `backend` / `worker` / `frontend` 五类服务及依赖顺序、健康检查、卷与端口 |
| [backend/Dockerfile](backend/Dockerfile) | Python 3.11 镜像；`alembic upgrade head` 后启动 uvicorn；安装 FlyAI CLI 与中文字体 |
| [frontend/Dockerfile](frontend/Dockerfile) | Node 22 构建阶段（`npm ci` + `npm run build`）+ Nginx 托管阶段；构建期注入 `VITE_AMAP_*` |
| `backend/app/core/config.py` | pydantic-settings `Settings`；含各 provider 环境变量与默认值 |
| `backend/alembic.ini` + `backend/alembic/` | 数据库迁移配置与迁移脚本 |
| `deploy/nginx/default.conf` | `/api/`、`/health` 反代到 `backend:8000`；SSE 关闭缓冲；SPA 路由回退 `index.html` |

> 凭据存放总则：后端 key 一律先写入根目录**未跟踪**的 `.env`，`docker compose up` 时注入容器；前端 `VITE_` 变量在构建期注入。`.env`、`Skills/**/apikey.txt`、`secretkey.txt`、`apipkey.txt` 已被 [.gitignore](.gitignore) 忽略，永不进入 git、SSE、日志或导出物。审计与指标接口只返回 provider 的健康/降级状态、耗时与错误码，不返回 key、Cookie 或请求体敏感字段。

---

## 4. 示例数据

| 路径 | 内容 |
| --- | --- |
| `shared/examples/wuhan-lushan-trip.json` | 示例行程快照「武汉—庐山两天一夜自然之旅」（`status=completed`，2 天，每天含 `stages` 与 `activities`），用于离线查看行程结构与导出模板 |
| `shared/schemas/*.json` | 前后端共享的 JSON Schema 契约（`Trip`、`DayPlan`、`MovementStage`、`Activity`、`PlanPatch`、`SourceRecord`、`VerificationIssue`、`SkillResult` 等），由 `backend/scripts/export_schemas.py` 生成 |
| `evaluation/scenarios.json` | 12 条需求理解评测场景（相对星期/英文日期/情侣人数/跨城同名/多模态/三餐住宿/补能/事件/闭环/跨海/著名地标/季节适配） |
| `evaluation/range_observations.simulated.json` | 12 组模拟传感器回放样本（标注 `simulation_sensor_replay`），用于续航误差计算链路验证 |
| `evaluation/results/*.json` | 基线评测结果（见 §6 运行证据） |

---

## 5. 部署说明

### 5.1 Docker Compose（推荐）

```powershell
# 1. 准备配置
Copy-Item .env.example .env
# 2. 在 .env 至少填入 DEEPSEEK_API_KEY 与 AMAP_WEBSERVICE_KEY
# 3. 启动（首次构建镜像并初始化数据库耗时几分钟）
docker compose up -d --build
# 4. 验证
docker compose ps
python deploy/api_smoke.py
```

服务依赖顺序保证健康后拉起下游：`backend` 依赖 `postgres` + `redis`，`worker` 依赖 `backend` + `postgres` + `redis`，`frontend` 依赖 `backend`。postgres/redis/backend 均配置 healthcheck。后端启动命令先 `alembic upgrade head` 再 uvicorn；backend 与 worker 共享 `UPLOAD_DIR=/app/data/uploads` 卷与 `FILE_RETENTION_DAYS`。

### 5.2 备份与恢复

- 备份：`deploy/scripts/backup.ps1` 用 `pg_dump --clean --if-exists` 导出带时间戳的 SQL（默认输出 `./backups`）。
- 恢复：`deploy/scripts/restore.ps1` 用 `psql -v ON_ERROR_STOP=1` 恢复，会覆盖当前数据库、须显式确认；恢复前先 `docker compose stop backend worker` 隔离写入，恢复后重启并核对 `/health`。

### 5.3 Nginx 与 HTTPS

`deploy/nginx/default.conf` 把 `/api/` 与 `/health` 反代到 `backend:8000`，`proxy_buffering off` 保证 SSE 不被缓冲；`/assets/` 走不可变长缓存，其余 SPA 路由回退 `index.html` 并禁止缓存。HTTPS 与局域网访问配置详见 [docs/operations.md](docs/operations.md)。

### 5.4 本地 Conda（开发）

```powershell
conda env create -f environment.yml
conda activate roadman
pip install -r requirements.txt
$env:PYTHONPATH = 'backend'
alembic -c backend/alembic.ini upgrade head
uvicorn app.main:app --reload --port 8000
```

前端另开终端：`cd frontend; npm install; npm run dev`。本地默认 SQLite；异步规划依赖 Redis 与 worker，可用 `docker compose up -d postgres redis worker` 只拉起依赖服务。

---

## 6. 测试方法与运行证据

### 6.1 自动化验证体系

| 层级 | 入口 | 覆盖内容 |
| --- | --- | --- |
| 后端单元与集成 | `pytest backend/tests -q` | 14 个测试文件、136 个测试函数：API、规划图、编辑、适配器、导出、旅游、每日复核、DeepSeek provider、Skill Registry |
| 前端状态测试与构建 | `npm run test` / `npm run build` | 规划事件去重、渐进显示、类型检查与生产构建 |
| 前端 E2E | `npm run test:e2e` | Playwright：`roadman.spec.ts`、`planning-agent.spec.ts`、`firefox-compat.spec.ts` |
| API 冒烟 | `python deploy/api_smoke.py` | 运行中容器的健康、CRUD、外部能力、规划启动、导出门禁 |
| 完整旅程验收 | `python deploy/full_journey_acceptance.py` | 新建 → 完成 → 语义编辑 → 重排 → 五格式导出 |
| 编辑重规划验收 | `python deploy/edit_replan_acceptance.py` | 补丁预览、确认、应用、重排与失败不污染正式行程 |
| 需求理解评测 | `python evaluation/run_evals.py` | 12 条场景的约束提取与结构校验 |
| 续航准确性 | `python evaluation/range_accuracy.py` | 能耗 MAPE、等效续航 MAPE、到达 SOC MAE |
| 安全与降级 | `python evaluation/safety_scenarios.py` | 低电量、恶劣天气、补能不足、车辆信息缺失、外部服务错误 |

### 6.2 已产出的运行证据

| 证据 | 数值（基线） |
| --- | --- |
| 需求理解评测 | 12 条场景 |
| 续航准确性（模拟回放） | 12 样本：能耗 MAPE **8.442%**、等效续航 MAPE **9.302%**、到达 SOC MAE **2.799 个百分点**（阈值 12% / 12% / 5pp，全部通过） |
| 安全与降级场景 | 5/5 命中预期；任务完成率 **100%**、路线可执行率 **80%**（补能设施仅估算位置时诚实标记为尚不可执行） |

> 续航基线使用 `simulation_sensor_replay` 数据，仅证明误差计算与 SOC 连续性链路可复现，**不构成真实车辆续航承诺**；接入真实路测时保留相同字段并将 `data_kind` 改为 `vehicle_telemetry`，二者需分开披露。安全场景的 `route_executability_rate` ≠ 任务完成率：补能只剩估算位置时系统完成"诚实降级"任务，但路线明确标记为尚不可执行，不会用虚假的 100% 掩盖风险。

### 6.3 运行证据与可复现性

运行证据包含后端测试、API 冒烟、需求评测、完整旅程验收与调用审计，均保留执行日期、Git SHA 与命令，隐藏密钥与个人路径。请求与追踪通过 `X-Request-ID`、`X-Trace-ID` 回传；Skill 调用经 Skill Registry 记录 provider、耗时、成功/缓存、错误码与来源摘要，不记录密钥或模型原始输出。OpenAPI 见 `/docs`，技能健康与指标见 `/api/v1/skills/health`、`/calls`、`/metrics`。

---

## 7. 对外部 API 的事实依据（官方文档）

| 能力 | 依据事实 | 官方文档 |
| --- | --- | --- |
| DeepSeek Chat Completions | `POST /chat/completions`，OpenAI 兼容；`base_url=https://api.deepseek.com`；模型 `deepseek-v4-flash`；思考模式 `{"thinking":{"type":"enabled"}}`（默认 enabled、默认 effort high）；`reasoning_effort=low/high/max`（`medium`/`xhigh` 映射为 `high`）；JSON 输出 `{"type":"json_object"}`（需在消息中显式指示模型输出 JSON，否则可能生成空白流或 `finish_reason=length` 截断）；思考模式忽略 `temperature`/`top_p`/`presence_penalty`/`frequency_penalty`；思维链经 `reasoning_content` 返回，RoadMan 不落库、不入日志 | [api-docs.deepseek.com](https://api-docs.deepseek.com/api/create-chat-completion/) |
| 高德地图 | WebService REST（`restapi.amap.com`）地理编码/POI/驾车/步行/骑行/公交/路况；浏览器地图用 JSAPI v2 + `key` + `securityJsCode` | [lbs.amap.com](https://lbs.amap.com/) |
| Open-Meteo | `GET https://api.open-meteo.com/v1/forecast`，参数 `latitude/longitude`（WGS84）、`hourly`、`daily`、`forecast_days`（默认 7、最多 16）、`timezone` 等；默认无需 key，`apikey` 仅商业用途需 `customer-` 前缀；数据源自多国气象机构（DWD、NOAA、ECMWF、Météo-France、JMA、KMA、CMA 等），非商业免费、商业/自托管需按其许可 | [open-meteo.com](https://open-meteo.com/en/docs) |
| OpenTripMap | `GET api.opentripmap.org/0.1/en/places/radius`；基于 OpenStreetMap（ODbL）与 Wikidata/Wikipedia 的开放地点，需保留来源与署名 | [opentripmap.io](https://opentripmap.io/) |
| 车型目录 | 公开接口 `https://tool.bitefu.net/car/`，只导入品牌/车系/车型身份信息 | [tool.bitefu.net/car](https://tool.bitefu.net/car/) |
| LangGraph | Python `langgraph>=1.0`；`StateGraph`、`add_node`、`add_edge`、`add_conditional_edges`、`compile`（RoadMan 使用 22 节点状态机），支持 checkpointer 持久化 | [langchain-ai.github.io/langgraph](https://langchain-ai.github.io/langgraph/) |

> 说明：以上 API 事实在撰写时取自对应官方文档；运行时可访问的 provider 状态以 `/api/v1/skills/health` 为准。DeepSeek 接口采用的 JSON 输出约定在 RoadMan 的 prompt 中已显式要求输出结构化 JSON，符合官方「JSON Output 需消息内显式指示」的要求。
